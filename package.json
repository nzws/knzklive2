{
  "name": "knzklive2",
  "version": "0.1.0",
  "repository": "https://github.com/nzws/knzklive2",
  "license": "AGPL-3.0",
  "private": true,
  "packageManager": "yarn@3.2.2",
  "workspaces": {
    "packages": [
      "./apps/*",
      "./packages/*"
    ]
  },
  "scripts": {
    "prettier:lint": "prettier --check .",
    "prettier:format": "prettier --write .",
    "dev": "nf start -x 8080,3000 app,web",
    "lint": "yarn prettier:lint && yarn workspaces foreach --verbose --parallel --exclude knzklive2 run lint",
    "format": "yarn prettier:format && yarn workspaces foreach --verbose --parallel --exclude knzklive2 run format",
    "test": "yarn workspaces foreach --verbose --parallel --exclude knzklive2 run test"
  },
  "devDependencies": {
    "@knzk/prettier-config": "^1.0.0",
    "foreman": "3.0.1",
    "prettier": "2.7.1"
  },
  "prettier": "@knzk/prettier-config"
}

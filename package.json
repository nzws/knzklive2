{
  "name": "knzklive2",
  "version": "0.1.0",
  "repository": "https://github.com/nzws/knzklive2",
  "license": "AGPL-3.0",
  "private": true,
  "packageManager": "yarn@3.2.2",
  "workspaces": {
    "packages": [
      "*"
    ]
  },
  "scripts": {
    "ws:lint": "prettier --ignore-path .gitignore --check .",
    "ws:format": "prettier --ignore-path .gitignore --write .",
    "dev": "nf start -x 8080,3000 app,web",
    "lint": "yarn workspaces foreach --verbose --parallel --exclude knzklive2 run lint",
    "format": "yarn workspaces foreach --verbose --parallel --exclude knzklive2 run format",
    "test": "yarn workspaces foreach --verbose --parallel --exclude knzklive2 run test"
  },
  "devDependencies": {
    "foreman": "3.0.1",
    "prettier": "2.7.1"
  }
}
